
// This file is now just a re-export to maintain backwards compatibility
// while we gradually update imports across the codebase
export * from '../types';

// Define Language type for better type safety
export type Language = 'tr' | 'en' | 'de';

